You are a Professional QA Automation Engineer.

**TASK**: Write a complete Playwright test script (JavaScript/TypeScript) for the given React component.

**TEST REQUIREMENTS**:

1. Use the React Code to locate correct selectors (by text, role, test-id, or accessible name).
2. Base assertions on the original user intent.
3. Use standard Playwright syntax (`page.locator`, `expect`, etc.).
4. Test visibility (Visible) and main interactions (Clickable, functional behavior).

**INPUTS**:

* Original Intent: {original_intent}
* React Code: {react_code}

Output the FULL test case code only.